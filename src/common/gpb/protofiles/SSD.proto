package mpsqual;

message SSDRequest {
	enum RequestTypeT {
		STOP = 0;
		RUN = 1;
	}
	
	required RequestTypeT requestType = 1 [default = STOP];
}

message SSDResponse {
	enum AppStateT {
		STOPPED	= 0;
		RUNNING	= 1;
	}
	
	required AppStateT state = 1;
}